# AI Critique

Trong quá trình thực hiện đồ án, AI đã bộc lộ nhiều sai lệch và "ảo giác" (hallucinations) nghiêm trọng. Cụ thể, AI tạo dữ liệu CSV tĩnh gây đụng độ (data collision) ở Spike Test và chèn thừa dấu ngoặc kép làm vỡ cú pháp JSON ở Stress Test. Nó mắc lỗi "Weak Assertions" khi chỉ tin vào HTTP 200 mà bỏ qua việc kiểm chứng Database. Khâu phân tích bộc lộ rõ nhất sự yếu kém khi AI đọc sai lệch log thô (nhầm Median với Max, tráo đổi Throughput giữa các kịch bản) và đưa ra đề xuất phi logic như tăng Connection Pool lên 500 cho SQLite. Nguyên nhân cốt lõi của những thất bại này là do AI thiếu tư duy bối cảnh toàn vẹn (End-to-End Context) và xử lý tác vụ cô lập. Nó sinh file CSV và JMeter độc lập mà không lường trước hậu quả khi nội suy dữ liệu. Đồng thời, AI áp dụng máy móc tối ưu MySQL sang SQLite do lỗ hổng nhận thức kiến trúc hệ thống, cũng như bị mất/tràn ngữ cảnh khi đọc nhiều file `.jtl` cùng lúc. Bài học quan trọng nhất em nhận ra khi cộng tác với AI là nguyên tắc "Evidence-Driven Human-in-the-Loop". AI là công cụ tạo phác thảo (generator) xuất sắc nhưng là người thẩm định (validator) tồi. Kỹ sư QA bắt buộc phải làm người gác cổng: tự tay dọn dẹp dữ liệu, thiết lập assertion chặt chẽ, và luôn dùng dữ liệu thô để phản biện lại AI, tuyệt đối không phó mặc hoàn toàn dự án cho các báo cáo tóm tắt tự động.
